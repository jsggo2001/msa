server:
  port: 8888
spring:
  application:
    name: config-service
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/dev/msa_all/native-file-repo
        git:
#          uri: file:///Users/jeongseung-gu/dev/msa_all/git-local-repo
          uri: https://github.com/jsggo2001/spring-cloud-config.git
#          username: {username}
#          password: {password}
#          username? password? when private git repository using.

management:
  endpoints:
    web:
      exposure:
        include: health, busrefresh
